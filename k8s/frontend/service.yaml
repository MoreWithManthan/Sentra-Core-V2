apiVersion: v1
kind: Service
metadata:
  name: sentra-frontend
  namespace: sentra
  labels:
    app: sentra-frontend
spec:
  selector:
    app: sentra-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
